## Oracle Room frontend config. Copy to .env.local and adjust.

# URL of the local API proxy server (see ../../server.ts, run via `npm run server` from the repo root).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
